#include <stdio.h>
#include <math.h>

int main() {
int j, value=1;

for(j=1;j<100; j++){
	if (value > 0){
		value *= 2;
		printf("%d %d\n", j, value);
	}
	else {
		break;
	}
}
value = value-1;

printf("The largest possible value is %d.\n", value);

return 0;
}
	
	
